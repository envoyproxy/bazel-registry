matrix:
  bazel:
  - 8.x

tasks:
  verify_targets:
    name: Verify build targets
    platform: ubuntu2404
    bazel: ${{ bazel }}
    build_targets:
    - '@hermetic_android_toolchains//:linux_x86_64'
