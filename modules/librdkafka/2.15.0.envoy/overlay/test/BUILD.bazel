load("@rules_cc//cc:cc_test.bzl", "cc_test")

cc_test(
    name = "extension_deps_test",
    srcs = ["extension_deps_test.cc"],
    deps = [
        "@librdkafka_deps//:crypto",
        "@librdkafka_deps//:ssl",
        "@librdkafka_deps//:zlib",
    ],
)
