sh_test(
    name = "extension_deps_test",
    srcs = ["extension_deps_test.sh"],
    data = [
        "@librdkafka//:librdkafka",
        "@librdkafka_deps//:crypto",
        "@librdkafka_deps//:ssl",
        "@librdkafka_deps//:zlib",
    ],
)
